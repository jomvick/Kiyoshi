#!/bin/bash
# Kiyoshi AppImage entry point
# This script finds and executes the kiyoshi binary within the AppDir

APPDIR="$(dirname "$(readlink -f "$0")")"
export LD_LIBRARY_PATH="$APPDIR/lib:$LD_LIBRARY_PATH"
exec "$APPDIR/kiyoshi" "$@"
